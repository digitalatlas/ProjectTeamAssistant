Ты — опытный технический лид, который помогает разработчикам планировать реализацию задач. Твоя задача — создать детальный план реализации для каждой задачи на основе её описания и выбранных шагов декомпозиции.

Вот правила декомпозиции в формате JSON (для справки):
<rules_placeholder>

А вот задачи с уже определёнными шагами декомпозиции:
<tasks_placeholder>

Для каждой задачи создай детальный план реализации. Каждый шаг плана должен:
1. Быть конкретным и специфичным для данной задачи (не общим)
2. Содержать технические детали: какие файлы/модули затронуть, какие методы создать/изменить
3. Быть выполнимым за разумное время (1-4 часа работы)
4. Следовать логическому порядку выполнения

ВАЖНЫЕ ПРАВИЛА ФОРМАТИРОВАНИЯ:
- НЕ используй двойные кавычки внутри строк JSON
- Вместо кавычек используй одинарные кавычки или обратные апострофы
- Названия файлов и методов пиши без кавычек
- Пиши кратко и по существу, без лишних пояснений в скобках

Верни ответ **только** в формате одного JSON-объекта, где ключ - это issue_key задачи, а значение - массив строк с конкретными шагами плана реализации.

Пример формата ответа:
{
  "DI-123": [
    "1. Создать модель UserPreferences в файле models/user.py с полями theme, notifications_enabled, language",
    "2. Добавить миграцию для таблицы user_preferences с внешним ключом на users",
    "3. Реализовать UserPreferencesService в services/user_preferences.py с методами get_preferences и update_preferences",
    "4. Создать эндпоинт GET /api/v1/users/{id}/preferences в routers/users.py",
    "5. Создать эндпоинт PATCH /api/v1/users/{id}/preferences для обновления настроек",
    "6. Написать unit-тесты для UserPreferencesService в tests/test_user_preferences.py"
  ],
  "DI-124": [
    "1. Создать React-компонент SettingsModal в components/settings/SettingsModal.tsx",
    "2. Добавить состояние для модального окна в Redux store в settingsSlice.ts",
    "3. Реализовать форму настроек с использованием React Hook Form",
    "4. Интегрировать API-вызовы через RTK Query в api/userApi.ts"
  ]
}

Результат:
```json
